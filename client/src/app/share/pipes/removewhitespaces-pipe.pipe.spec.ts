import { RemovewhitespacesPipePipe } from './removewhitespaces-pipe.pipe';

describe('RemovewhitespacesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RemovewhitespacesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
